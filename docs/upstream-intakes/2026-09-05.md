# Upstream intake 2026-09-05

Status: source resolution resumed on the original frozen inputs after explicit orchestrator authorization. Develop advancement remains documented below. Acceptance incomplete. Do not merge.

## Frozen inputs

- Downstream: `78df8602e8aa5a482da50d46ec8cf6d175d36535`
- Upstream: `2e0614fc2b64c44f5e62b5983f0ebbc03ff5a3e5`
- Merge base: `ab6c1f2d2c3695f7023f85692b70d388fe63a018`, 2026-08-31 17:59:00 +0530
- Fresh fetch matched both user-observed candidates. Base ancestry checks passed on both sides.
- Rollback tag: `pre-upstream-merge-20260905`, pushed to origin.
- Exclusive commits: upstream 73; downstream 195.
- Downstream delta: 331 files, 59,645 insertions, 18,923 deletions.
- Upstream delta: 812 files, 106,704 insertions, 13,160 deletions.
- Shared files: 96; downstream only: 235; upstream only: 716.
- Merge conductor: hosted-ao-90. Freeze and reviewer staffing requested from hosted-ao-44.
- Acceptance environment: local macOS workspace plus configured CI/platform runners; packaged remote-machine infrastructure availability must be established.
- Landing: GitHub merge commit only. Never squash or rebase. PR remains unmerged.

## Implementation plan

1. Preserve upstream ancestry with one `git merge --no-ff upstream/main`.
2. Resolve source conflicts by playbook ownership, recording every decision.
3. Regenerate API and sqlc output; inspect migrations and hosted boundaries.
4. Run relevant full CI, race, platform and packaged acceptance checks; record external blockers.
5. Request independent backend/domain+migration, desktop/frontend, and release/acceptance reviews on the coordinated exact head.

README issue #149 and unrelated feature work are excluded.

## Shared conflict surface

- `.github/workflows/go.yml`
- `AGENTS.md`
- `backend/internal/adapters/runtime/conpty/ptyregistry/ptyregistry_test.go`
- `backend/internal/adapters/runtime/conpty/ptyregistry/registry.go`
- `backend/internal/cli/root.go`
- `backend/internal/cli/root_test.go`
- `backend/internal/config/config.go`
- `backend/internal/config/config_test.go`
- `backend/internal/daemon/daemon.go`
- `backend/internal/httpd/api.go`
- `backend/internal/httpd/apispec/openapi.yaml`
- `backend/internal/httpd/apispec/specgen/build.go`
- `backend/internal/httpd/controllers/dto.go`
- `backend/internal/httpd/cors.go`
- `backend/internal/httpd/cors_test.go`
- `backend/internal/service/project/service.go`
- `backend/internal/service/project/service_test.go`
- `backend/internal/session_manager/manager.go`
- `backend/internal/session_manager/manager_test.go`
- `backend/internal/storage/sqlite/db.go`
- `backend/internal/storage/sqlite/gen/app_settings.sql.go`
- `backend/internal/storage/sqlite/gen/models.go`
- `backend/internal/storage/sqlite/store/app_settings_store.go`
- `backend/internal/telemetrymeta/cli.go`
- `backend/sqlc.yaml`
- `docs/STATUS.md`
- `docs/architecture.md`
- `docs/cli/README.md`
- `docs/telemetry.md`
- `frontend/e2e/smoke-datadir.spec.ts`
- `frontend/e2e/smoke-t0.spec.ts`
- `frontend/e2e/support/fake-bridge.ts`
- `frontend/forge.config.test.ts`
- `frontend/forge.config.ts`
- `frontend/package-lock.json`
- `frontend/package.json`
- `frontend/src/api/schema.ts`
- `frontend/src/main.ts`
- `frontend/src/main/auto-updater.test.ts`
- `frontend/src/main/auto-updater.ts`
- `frontend/src/main/cloud-auth.ts`
- `frontend/src/preload.ts`
- `frontend/src/renderer/__tests__/integration/board-empty-states.test.tsx`
- `frontend/src/renderer/__tests__/integration/pr-hydration.test.tsx`
- `frontend/src/renderer/components/BrowserPanel.test.tsx`
- `frontend/src/renderer/components/CloneRepositoryDialog.tsx`
- `frontend/src/renderer/components/CommandPalette.test.tsx`
- `frontend/src/renderer/components/ConnectMobileModal.telemetry.test.tsx`
- `frontend/src/renderer/components/ConnectMobileModal.test.tsx`
- `frontend/src/renderer/components/CreateProjectFlow.tsx`
- `frontend/src/renderer/components/GlobalSettingsForm.tsx`
- `frontend/src/renderer/components/NewTaskDialog.test.tsx`
- `frontend/src/renderer/components/ProjectSettingsForm.test.tsx`
- `frontend/src/renderer/components/SessionInspector.test.tsx`
- `frontend/src/renderer/components/SessionView.test.tsx`
- `frontend/src/renderer/components/SessionsBoard.test.tsx`
- `frontend/src/renderer/components/SessionsBoard.tsx`
- `frontend/src/renderer/components/SettingsDialog.test.tsx`
- `frontend/src/renderer/components/SettingsDialog.tsx`
- `frontend/src/renderer/components/ShellTopbar.test.tsx`
- `frontend/src/renderer/components/Sidebar.test.tsx`
- `frontend/src/renderer/components/TaskComposer.test.tsx`
- `frontend/src/renderer/components/TerminalPane.test.tsx`
- `frontend/src/renderer/components/chat/SessionChatSurface.test.tsx`
- `frontend/src/renderer/components/chat/TurnSettingsBar.tsx`
- `frontend/src/renderer/hooks/useAgentsQuery.ts`
- `frontend/src/renderer/hooks/useConversation.test.tsx`
- `frontend/src/renderer/hooks/useDaemonStatus.test.tsx`
- `frontend/src/renderer/hooks/useShellTerminals.test.tsx`
- `frontend/src/renderer/hooks/useShellTerminals.ts`
- `frontend/src/renderer/hooks/useWorkspaceQuery.test.tsx`
- `frontend/src/renderer/hooks/useWorkspaceQuery.ts`
- `frontend/src/renderer/i18n/de.json`
- `frontend/src/renderer/i18n/en.json`
- `frontend/src/renderer/i18n/es.json`
- `frontend/src/renderer/i18n/fr.json`
- `frontend/src/renderer/i18n/ja.json`
- `frontend/src/renderer/i18n/ko.json`
- `frontend/src/renderer/i18n/pt-BR.json`
- `frontend/src/renderer/i18n/zh-CN.json`
- `frontend/src/renderer/lib/api-client.test.ts`
- `frontend/src/renderer/lib/api-client.ts`
- `frontend/src/renderer/lib/bridge.ts`
- `frontend/src/renderer/lib/event-transport.test.ts`
- `frontend/src/renderer/lib/event-transport.ts`
- `frontend/src/renderer/lib/notifications.test.ts`
- `frontend/src/renderer/lib/notifications.ts`
- `frontend/src/renderer/lib/spawn-orchestrator.test.ts`
- `frontend/src/renderer/lib/workspace-file-events.ts`
- `frontend/src/renderer/main.tsx`
- `frontend/src/renderer/routes/_shell.tsx`
- `frontend/src/renderer/stores/ui-store.ts`
- `frontend/src/renderer/styles.css`
- `frontend/src/renderer/test/setup.ts`
- `frontend/src/shared/telemetry.ts`
- `frontend/vite.renderer.config.ts`

## Preflight conflicts

- CONFLICT (content): Merge conflict in .github/workflows/go.yml
- CONFLICT (content): Merge conflict in AGENTS.md
- CONFLICT (content): Merge conflict in backend/internal/cli/root.go
- CONFLICT (content): Merge conflict in backend/internal/daemon/daemon.go
- CONFLICT (content): Merge conflict in backend/internal/httpd/apispec/openapi.yaml
- CONFLICT (content): Merge conflict in backend/internal/httpd/apispec/specgen/build.go
- CONFLICT (content): Merge conflict in backend/internal/service/project/service.go
- CONFLICT (content): Merge conflict in backend/internal/storage/sqlite/db.go
- CONFLICT (content): Merge conflict in frontend/e2e/smoke-t0.spec.ts
- CONFLICT (content): Merge conflict in frontend/e2e/support/fake-bridge.ts
- CONFLICT (content): Merge conflict in frontend/forge.config.ts
- CONFLICT (content): Merge conflict in frontend/src/api/schema.ts
- CONFLICT (content): Merge conflict in frontend/src/main.ts
- CONFLICT (content): Merge conflict in frontend/src/main/cloud-auth.ts
- CONFLICT (content): Merge conflict in frontend/src/renderer/components/CloneRepositoryDialog.tsx
- CONFLICT (content): Merge conflict in frontend/src/renderer/components/CreateProjectFlow.tsx
- CONFLICT (content): Merge conflict in frontend/src/renderer/components/GlobalSettingsForm.tsx
- CONFLICT (content): Merge conflict in frontend/src/renderer/components/NewTaskDialog.test.tsx
- CONFLICT (content): Merge conflict in frontend/src/renderer/components/SessionView.test.tsx
- CONFLICT (content): Merge conflict in frontend/src/renderer/components/SessionsBoard.tsx
- CONFLICT (content): Merge conflict in frontend/src/renderer/components/SettingsDialog.test.tsx
- CONFLICT (content): Merge conflict in frontend/src/renderer/components/SettingsDialog.tsx
- CONFLICT (content): Merge conflict in frontend/src/renderer/components/Sidebar.test.tsx
- CONFLICT (content): Merge conflict in frontend/src/renderer/components/chat/SessionChatSurface.test.tsx
- CONFLICT (content): Merge conflict in frontend/src/renderer/components/chat/TurnSettingsBar.tsx
- CONFLICT (modify/delete): frontend/src/renderer/hooks/useAgentsQuery.ts deleted in 2e0614fc2b64c44f5e62b5983f0ebbc03ff5a3e5 and modified in 78df8602e8aa5a482da50d46ec8cf6d175d36535.  Version 78df8602e8aa5a482da50d46ec8cf6d175d36535 of frontend/src/renderer/hooks/useAgentsQuery.ts left in tree.
- CONFLICT (content): Merge conflict in frontend/src/renderer/hooks/useShellTerminals.test.tsx
- CONFLICT (content): Merge conflict in frontend/src/renderer/i18n/de.json
- CONFLICT (content): Merge conflict in frontend/src/renderer/i18n/en.json
- CONFLICT (content): Merge conflict in frontend/src/renderer/i18n/es.json
- CONFLICT (content): Merge conflict in frontend/src/renderer/i18n/fr.json
- CONFLICT (content): Merge conflict in frontend/src/renderer/i18n/ja.json
- CONFLICT (content): Merge conflict in frontend/src/renderer/i18n/ko.json
- CONFLICT (content): Merge conflict in frontend/src/renderer/i18n/pt-BR.json
- CONFLICT (content): Merge conflict in frontend/src/renderer/i18n/zh-CN.json
- CONFLICT (content): Merge conflict in frontend/src/renderer/lib/api-client.ts
- CONFLICT (content): Merge conflict in frontend/src/renderer/lib/bridge.ts
- CONFLICT (content): Merge conflict in frontend/src/renderer/lib/event-transport.test.ts
- CONFLICT (content): Merge conflict in frontend/src/renderer/lib/event-transport.ts
- CONFLICT (content): Merge conflict in frontend/src/renderer/lib/notifications.ts
- CONFLICT (content): Merge conflict in frontend/src/renderer/routes/_shell.tsx
- CONFLICT (content): Merge conflict in frontend/src/renderer/stores/ui-store.ts
- CONFLICT (content): Merge conflict in frontend/src/shared/telemetry.ts

## Conflict decisions

- `.github/workflows/go.yml` (shared): retain downstream affected-check gating; accept upstream 15-minute race timeout and focused Windows workspace/session-kill job.
- `frontend/forge.config.ts` (shared): retain Hosted AO executable, bundle identity, release repository and absence of upstream auth protocol registration; accept upstream better-sqlite3 native dependency inclusion, rebuild hook, unpack plugin and packaged-runtime assertion. Existing unsigned build configuration and macOS zip/dmg makers remain.
- `AGENTS.md` (shared): retain hosted gateway modes and hosted state isolation; tentatively include upstream exact GET-only LAN identity exemption, pending backend review against current HAO contracts. Gateway authentication remains required. Retain hosted restriction on OS-default app-state reads; browser import implications require desktop review.
- Remaining backend and desktop conflicts: delegated through AO to hosted-ao-91 and hosted-ao-92; decisions pending.
- Generated artifacts will be regenerated from resolved source, not hand-merged.

## Verification and invariant attestations

Pending. No build, test, migration, transport, packaging, or acceptance success is claimed.

## Execution notes

- Draft PR: https://github.com/agentlab-in/hosted-ao/pull/150
- GitHub supports merge commits but also allows squash/rebase repository-wide. This PR explicitly requires merge-commit landing; repository-wide options were not changed.
- Local frontend `npm ci` initially blocked by host npm EALLOWGIT policy for pinned `@electron/node-gyp` Git dependency brought by upstream `@electron/rebuild`. No policy bypass applied; decision requested from conductor.

## Verification in progress

- `npm run shared:check`: passed (Cloud schema drift, typecheck, tests, build; product-ui typecheck, tests, build).
- `node --test scripts/ao-e2e-pod-gate.test.mjs`: passed, 17 tests.
- ACP runtime helpers use Vitest; execution awaits frontend dependency installation.
- Textual packaging seam review: macOS zip and dmg makers retained; remote HTTPS/WSS CSP entries retained. Runtime acceptance pending.
- Baseline `frontend-release.yml` has optional signing-secret branches and `build-artifacts.yml` requires a WorkOS variable. Neither workflow is changed by this upstream delta. These are pre-existing policy/infrastructure follow-ups, not permission to sign or publish from this intake.

- Focused race tests: domain and vmgateway passed; config failed because the clean-merged `TestLoadDefaults` expected bare `~/.ao`. Correction assigned to backend resolution worker; rerun required.
- Local `DAYTONA_API_KEY` and `VITE_WORKOS_CLIENT_ID` are absent. No credential values inspected or fabricated.
- Initial GitHub checks apply only to the preflight documentation commit, not the in-progress intake. They are not acceptance evidence for the merge.
- macOS feed generation still omits blockmap sidecars to force full zip downloads (`frontend/scripts/feed.mjs`); this file is unchanged by the intake.

## Upstream summary

- Session lifecycle: asynchronous teardown, restore fallback, agent-switch failure observability, conflict nudges, and open-PR status correction.
- Agent setup: provider model catalogs, Codex accounts/global switching, harness login and reviewer defaults.
- Project and browser experience: project import validation/onboarding, missing-folder removal, browser navigation/profile import, file diff preview and navigation refinements.
- Desktop updates: discover completed feature prerelease feeds, separate update discovery from automatic download settings.
- Mobile/cloud: identity probing, upstream cloud development infrastructure and transport changes; hosted identity and gateway boundaries remain separately owned.
- Packaging/tooling: SQLite native runtime bundling, ACP source fingerprints, dependency and pricing catalog refreshes, Windows teardown tests.

## Freeze breach

Remote `develop` advanced after freezing to `9b3b126cfd882d1cc2a54650324e58ed55e7a1e7`, a merge of PR #151 (`docs: refresh README for Hosted AO and HAO`). Its first parent is the frozen `78df8602e8aa5a482da50d46ec8cf6d175d36535`; second parent is `269f005f76cd659b9f2042f73a26d477246ae11a`.

This is the unrelated README work explicitly excluded from this intake. The conductor paused integration and requested a decision. Frozen local refs, rollback tag, and unresolved true merge remain intact. No README changes were added by the conductor, no merge commit was completed, and no upstream intake acceptance is claimed. Resolution workers were told to preserve their frozen-input work and not move inputs.

## Resume authorization

Orchestrator hosted-ao-44 explicitly authorized proceeding at the original downstream `78df8602e8aa5a482da50d46ec8cf6d175d36535` and upstream `2e0614fc2b64c44f5e62b5983f0ebbc03ff5a3e5`. Conductor retained those inputs and notified the orchestrator that README PR #151 had already advanced develop. The original true merge resumed; README work is not imported into the sync branch.

- Pricing catalog tooling tests passed and the frozen catalog validates (`go test ./cmd/pricingcatalog ./internal/pricing/catalogsync`; `go run ./cmd/pricingcatalog validate -root ..`).
- Supplemental local Gitleaks default-rule scan of the 73 upstream commits flagged two test fixtures (Codex idempotency key assertion and QR pairing-password fixture). This differs from CI's pinned v7.4 custom-rule scan; no clean secret-scan claim is made for the final head yet.
- Repository Actions variable list is empty, confirming the unchanged artifact workflow's mandatory WorkOS-variable guard cannot currently pass. Docker 28.1.1 is available locally.

## Confirmed Mobile boundary decision

Preserve current HAO policy: Connect Mobile stays home-network-only and every LAN request remains authenticated. Upstream public cloudflared tunnel wiring and unauthenticated LAN identity exemption are intentionally disabled at this hosted boundary. The earlier tentative AGENTS identity-exemption resolution is superseded. Backend and desktop workers are coordinating source, UI, tests and denylist parity; generated artifacts will reflect resolved source.

## External acceptance blocker confirmed by orchestrator

hosted-ao-44 audited repository configuration and found no Actions variables or secret names. `VITE_WORKOS_CLIENT_ID` is not configured, and no verified production-faithful remote-machine acceptance host is available. Packaged/remote acceptance is externally blocked until the human configures a real nonsecret client ID and supplies a reachable acceptance host. Do not fabricate values, reuse stale credentials or dispatch the artifact workflow before both a coherent head and required configuration exist.

The orchestrator reconfirmed the original frozen downstream base and will assign three independent reviewers only after the coordinated exact head is supplied. PR remains merge-commit-only and unmerged.

## Dependency verification decision

The orchestrator explicitly prohibits bypassing or relaxing local npm EALLOWGIT. Frontend install is locally policy-blocked by upstream `@electron/rebuild` requiring `@electron/node-gyp` at commit `06b29aafb7708acef8b3669835c8a7857ebc92d2`. Use ordinary GitHub frontend CI for dependency installation, typecheck and tests after the coherent head is pushed. If CI fails too, record the exact job failure and request a scoped dependency/provenance decision. Do not change host policy or dependency versions to evade the restriction.

This is separate from the packaged workflow's missing WorkOS configuration and unavailable remote acceptance host. Root API-generation dependencies installed successfully; API/sqlc regeneration can proceed locally after source resolution.

## Release workflow non-diff evidence

`git diff --cached --exit-code 78df8602e8aa5a482da50d46ec8cf6d175d36535 -- .github/workflows/frontend-release.yml .github/workflows/build-artifacts.yml` passed with empty output during resolution. The WorkOS guard and optional signing paths in those workflows are unchanged baseline follow-ups, not intake-caused code defects. Repeat this check on the final committed head.

Independent release/acceptance review must verify that changes to Forge native packaging, ACP bundling and updater behavior do not activate or broaden signing. Exact-head packaged acceptance remains externally pending where missing configuration prevents artifact builds. The gate-verdict contract suite passed 17/17 locally; that result is not packaged-app acceptance.

## Browser profile import boundary

Explicit orchestrator decision: this intake does not authorize reading, scanning or importing OS-default app-data roots, including `~/Library/Application Support`. Hosted browser behavior may access only its existing `~/.ao/hosted/electron` state; external profile import must remain unavailable/disabled with truthful UX. User-selected external imports require a separate security/product issue and review. Desktop resolution must guard discovery/import entrypoints before filesystem access and test the restriction.

CodeGraph initialization is explicitly excluded from this intake. Continue with available analysis tools. Local npm EALLOWGIT remains unchanged; ordinary CI is the verification path.

## Backend patch integration and regeneration

Applied backend worker patches 01 and 02 with applicability checks inside the existing true merge. Detailed decisions are in `2026-09-05-backend-decisions.md`. Renamed the LAN integration test to match its retained authentication requirement.

API generation succeeded. Initial sqlc generation failed because frozen upstream `backend/sqlc.yaml` contains tab-indented overrides at lines 246/247 and a missing type for `agent_switch_failure_policy.enabled`. Resolved the source to a valid boolean override and removed the duplicate `app_settings.cloud_offering` entry; the existing hosted boolean override is retained. No generated code was hand-edited.

The new Codex account-management prefix stays loopback-only under the gateway denylist. Remote UI must not suggest this SSE/account surface is available through a hosted or paired gateway. The POST-only agent-install guard is enforced and tested in Go; the existing path-only contract format is unchanged and this method guard is documented separately.

## Governing backend policy (v2 plus conductor classification)

The orchestrator selected backend handoff v2. This supersedes earlier all-LAN-authenticated resolution notes: retain upstream's exact GET-only LAN identity exemption, with its tests and ADR. Gateway authentication remains mandatory. Public cloudflared production wiring remains omitted.

Every Codex route registered in `CodexAccountsController` (account listing/events, ensure, reset credit, login/logout/delete, login-operation verify/cancel, account switch/recovery) is local-only and denied by prefix at LAN and public gateways. No cookie exception is added for account SSE.

POST `/agents/{agent}/install` (including reinstall) and POST `/agents/{agent}/verify` are local controls, denied before auth/proxy on both boundaries. GET `/agents/installers`, `/agents/install-jobs`, and `/agents/{agent}/install` are read-only installer catalog/status surfaces and remain authenticated application API. The new `/desktop/sessions/{sessionId}/workspace` filesystem-location surface is also gateway-denied, matching LAN.

Real hosted and pair gateway tests cover missing, invalid and valid credentials, spoofed localhost Host, response status and zero daemon calls for denied routes. Method-specific LAN tests prove inner authentication/router is not invoked for controls. The path-only contract records prefix denials; POST-only install/verify guards remain explicitly documented and tested in Go.

## Authorized merge topology

Orchestrator accepted the published docs-only intermediate first parent `df8e86498b84570b9e677b555aaa6ddc993a7db0`. Its sole parent is frozen downstream `78df8602e8aa5a482da50d46ec8cf6d175d36535`; its only changed path is this intake evidence document. These checks were verified before committing.

The existing genuine `git merge --no-ff --no-commit upstream/main` will be committed with ordered parents `df8e86498b84570b9e677b555aaa6ddc993a7db0` and `2e0614fc2b64c44f5e62b5983f0ebbc03ff5a3e5`. This preserves the original frozen downstream as an ancestor without rewriting the published preflight commit or restarting the merge. A separate normal merge will then import widened develop `9b3b126cfd882d1cc2a54650324e58ed55e7a1e7` and resolve README integration.

The merge commit is an integration checkpoint, not acceptance: regenerated sqlc store types still require a source correction, external browser import must be disabled, and full final-head checks/reviews remain outstanding.

Desktop archive integration verified every manifest SHA-256, applying 38 paths including the obsolete hook deletion. Three proposed helper paths were excluded: `forge.config.test.ts`, `auto-updater.ts`, and `auto-updater.test.ts`; the conductor retained their clean merged content because helper changes required unrelated baseline signing work or duplicated existing macOS full-download feed policy. Historical helper tests ran against an isolated candidate and do not certify this head; its recorded npm bypass is not authorized or reused by the conductor.

## Verified merge commits

- True upstream merge: `f78386e660d9148e76e5143e825a1de1d31244b7`; ordered parents `df8e86498b84570b9e677b555aaa6ddc993a7db0`, `2e0614fc2b64c44f5e62b5983f0ebbc03ff5a3e5`.
- Separate develop integration: `321c625a26389a9b2b371e60453063430c191b9e`; ordered parents `f78386e660d9148e76e5143e825a1de1d31244b7`, `9b3b126cfd882d1cc2a54650324e58ed55e7a1e7`.
- Both the intake merge and widened develop are verified ancestors of the combined head. The initial docs commit's sole parent and docs-only delta were verified above.
- README integration was textually clean. One claim was corrected: upstream now provides local Codex account/login controls, so the blanket statement that provider authentication never moves into AO was replaced with a precise local-controls description.
- Gateway expanded race suite passed. Full integrated checks remain incomplete due to the sqlc source correction and desktop policy follow-ups. Pushing an integration checkpoint triggers normal CI but does not freeze a review/acceptance head.

## External browser import disabled

Conductor disabled the live IPC discovery/import entrypoints before invoking `BrowserProfileImportService`. Discovery returns no sources; import rejects with an explicit Hosted-unavailable error, including for the trusted shell. The settings import control is disabled with visible explanatory text and no import dialog. Existing Hosted profile creation/history behavior remains available. Regression assertions prove neither importer discovery nor import is called, so these entrypoints cannot enumerate external roots or open source SQLite data. UI assertions cover the disabled control and zero discovery/import calls. Execution awaits normal frontend CI; no local npm bypass was used.

## Final governing security policy

Orchestrator explicitly superseded the intermediate v2 advice: LAN identity requires authentication, with no anonymous exemption. The conductor restored this boundary and its tests. All earlier identity-exemption notes are historical and rejected by this final decision. V2's non-policy source resolutions remain, while weaker policy advice is not used.

The entire Codex credential/control prefix and entire `/api/v1/system/install` family remain denied before auth/proxy at LAN and public gateways for every method. Existing segment-boundary sibling behavior is preserved. Additional POST agent install/verify guards cover installer aliases outside the full system-install family. Production cloudflared remains disabled. sqlc/migration source corrections are independent of these security rules.

## Mobile UI resolution

Applied helper supplemental `03-mobile-no-cloudflared.patch` after its applicability check. It removes the production connector installer and `/mobile/remote-access` mutation while retaining LAN enable/password/QR controls. Conductor corrected the unavailable copy to state the Hosted home-network-only policy rather than incorrectly blame a missing binary. Historical helper mobile results (38 passed, 6 skipped) are isolated-candidate evidence, not final-head CI.

## Released reviewer-history repair

Applied v3's source-only migration repair and tests after patch applicability checks, retaining the conductor's stricter current gateway/auth files. A released upstream helper could falsely record migration 118 when adding the reviewer column owned by 121. The repair checks physical cancelled-turn schema before removing a false 118 record and records 121 transactionally; tests cover already-false history and preservation of genuine 118 ledger entries. No shipped migration file is edited. Integrated execution still awaits sqlc compile repair.

Route audit also classifies POST `/agents/{agent}/auth` as credential control, denied at LAN/gateway. GET auth-plan descriptions and readiness probes remain authenticated diagnostic APIs. Gateway regression coverage includes the credential-launch alias.

## Tightened installer-family ruling

The orchestrator superseded the earlier authenticated read-only installer allowance. Entire `/api/v1/system/install` remains blocked for all methods. Installer aliases `/api/v1/agents/installers`, `/api/v1/agents/install-jobs`, and `/api/v1/agents/{agent}/install` or `/verify` (including descendants) are also local-only for every method. V3 positive assertions allowing those endpoints were rejected and replaced with blocked-route assertions. Segment-boundary siblings remain ordinary authenticated API.

V3 archive comparison found twelve files already matching and four policy/test differences. Its source repairs and contract fixtures were integrated, its identity lockout assertions retained, and its preflight denial combined with the conductor's broader login/verify/desktop restrictions. Additional concrete-route and all-credential tests were preserved separately. No generated code or shipped migration was hand-edited.

## Remote desktop control resolution

Applied supplemental desktop patch 04 after applicability check. Remote Codex account SSE is never opened and any local account stream closes on machine switch; ordinary CDC keeps credentials. Credential/installer settings show a local-machine requirement. Conductor corrected helper's stale POST-only system guard to full-family denial, blocks installer aliases and desktop controls, and retains ordinary readiness/auth-plan diagnostics. Renderer rejects local-only control requests before acquiring a remote bearer or calling fetch; tests cover this and normal authenticated remote REST. These are defense-in-depth UI rules; gateway enforcement remains authoritative.

## First normal-CI results

Normal GitHub frontend CI installed the pinned dependencies and reached Vitest/renderer smoke execution without a host-policy bypass. The initial checkpoint failed on missing translations for the new Hosted Mobile message, punctuation-sensitive UI assertions, and stale clone-flow smoke locators. Added the message to all seven remaining locales; desktop helper is addressing the assertions/flow checks against the merged UI. Go/build/API jobs confirm the same regenerated agent-switch type failure seen locally. These are failed intermediate checks, not acceptance.

Reference runs: Frontend `33920239141`; Go `33920239144`. Exact final head must pass fresh checks after corrections. Secret-scan check on the earlier integration checkpoint passed, but will be required again on the final head.

## sqlc projection correction

The regenerated AgentSwitch model puts the migration-added `failure_point` column last, while five SELECT projections placed it before timestamps. sqlc consequently emitted distinct row types incompatible with the store's AgentSwitch boundary. Reordered only those SELECT projections to canonical table order, preserving selected columns and INSERT bindings, then regenerated sqlc. The store and HTTP/CLI/service packages compile and their focused checks pass; full migration and full-suite results are recorded separately when complete.

## Independent review assignments required

Backend/domain+migration review must examine false-118 repair versus canonical 121 across fresh/populated/restart histories, unchanged released migrations, gateway all-method installer/Codex denials and manifest parity. Backend and desktop reviewers must both examine exact Codex SSE path, children, mutation routes, cookie ineligibility and segment-boundary siblings, while ordinary CDC/SSE/WSS authentication remains functional. Companion decision: `2026-09-05-codex-stream-decision.md`; helper race evidence: hosted-ao-91 `.intake-support/artifacts-v3/verification-codex-stream.log` (support fixture only).

## Verification correction checkpoint

At pushed checkpoint `877b3a321`, normal CI successfully installed frontend dependencies and passed API drift, Go lint, Windows workspace tests, native smoke on macOS/Linux/Windows, Linux binary builds, container smoke and secret scan. Frontend run `33920963926` still failed on five punctuation-sensitive expectations, one unlocalized local-machine message and three clone smoke cases. The four affected test files now expect the merged English text; localization and clone readiness fixtures remain pending. These results do not certify a final review head.

Two overlapping local full-suite processes used the same deterministic tmux test session name and destroyed each other's supervised session. An isolated clean-environment tmux race run passed. Subsequent full verification must avoid overlapping tmux runs. The installer cancellation test also used a 50 ms deadline that could expire before TLS download and scratch creation. It now cancels on script output, asserts execution began, then verifies scratch cleanup; ten race repetitions pass. Production installer behavior is unchanged.

Local HTTP shutdown regression was independently reproduced twice in three isolated race repetitions. Temporary connection-state instrumentation showed the default client transport speculatively dialing an unused `StateNew` connection while reusing its health-check connection for shutdown. That idle dial survived until the five-second server drain deadline. The health test helper now uses a separate non-keepalive transport; ten race repetitions pass. This changes test infrastructure only, not daemon shutdown or listener policy. Temporary instrumentation was removed.

After the shutdown test correction, `go test -race ./internal/httpd/...` passes. Isolated supervised tmux lifecycle test passes (5.245 seconds). Domain/config/gateway race checks pass with Codex stream cookie coverage. Repeated `npm run sqlc` and `npm run api` leave all generated paths unchanged. Local cross-build process completed successfully for Linux amd64, Windows amd64, macOS arm64 and macOS amd64.

Localized the remote-control explanation across all eight renderer locales. Clone smoke fixtures now answer the current GET readiness and POST readiness/ensure APIs using the existing typed readiness fixture; project-clone wire and error assertions remain unchanged. Normal frontend CI is the execution authority for these changes. The phone pairing seam remains pending: upstream v2 probes identity before supplying a token, incompatible with the retained fully authenticated LAN boundary. No anonymous exception was introduced.

Checkpoint d6f927e frontend smoke still found the clone submission label had changed from `Clone and start` to `Clone` in the integrated source. Updated the exact button locators, including the invalid-input assertion, while preserving clone wire/error/progress assertions. Fresh CI is required. Requested desktop patch 05 was re-audited at SHA-256 `eef3f7bed3549365e5e0e043a445a44fe70da75fd31fba265af609d780ae8602` and still fails governing policy prerequisites (Forge policy tests, redundant updater change, POST-only installer rule, absent external-import gate); it was not applied over the secured tree.
